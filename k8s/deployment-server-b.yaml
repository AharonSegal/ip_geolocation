apiVersion: apps/v1
kind: Deployment 
metadata: 
  name: server-b-deploy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: server-b
  template:
    metadata:
      name: pod-b
      labels:
        app: server-b
    spec:
       containers:
         - name: con-b
           image: moshe5434/black-project:v2 
           ports:
            - containerPort: 8000
           env:
            - name: REDIS_HOST
              value: "redis" # שם ה-Service של ה-Redis
            - name: REDIS_PORT
              value: "6379"
            - name: REDIS_DB
              value: "0"